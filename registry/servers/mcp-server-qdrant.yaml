name: mcp-server-qdrant
repo: qdrant/mcp-server-qdrant
language: python
grade: A
score: 97
checked_with: mcp-migrate 0.4.0
spec: "2026-07-28"
# backfilled 2026-08-09: re-verified against current HEAD, not the
# commit this entry was originally graded from -- that predates the
# `sha` field. See #223.
sha: f1a4d04e4f91c4d3e2b7c63d5283f3f4338fd2e5
status: ready
submitted_by: project
notes: >-
  Official MCP server for Qdrant that acts as a semantic memory layer for keeping and retrieving memories in the vector search engine.
