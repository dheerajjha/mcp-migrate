name: mcp-server-tree-sitter
repo: wrale/mcp-server-tree-sitter
language: python
grade: B
score: 94
checked_with: mcp-migrate 0.3.0
spec: "2026-07-28"
# backfilled 2026-08-09: re-verified against current HEAD, not the
# commit this entry was originally graded from -- that predates the
# `sha` field. See #223.
sha: 10c78c9d8aaf10d26ba550edbcdb822e87147bf8
status: ready
submitted_by: project
notes: >-
  MCP server providing tree-sitter code analysis so AI assistants get structure-aware access to codebases in many languages.
