name: mcp-server-git
repo: modelcontextprotocol/servers
path: src/git
language: python
grade: C
score: 66
checked_with: mcp-migrate 0.7.0
spec: "2026-07-28"
# backfilled 2026-08-09: re-verified against current HEAD, not the
# commit this entry was originally graded from -- that predates the
# `sha` field. See #223.
# re-graded 2026-09-14 at the same sha, D/58 -> C/66: this server uses
# Roots, and R007 and R018 were both reporting it, twice each from
# different lines. They merge on the feature now (#221), so one fact is
# one finding. The server did not change; the double-count did.
sha: 76d64c822f5125032f89eb71dbdb94e42b434821
status: ready
submitted_by: project
notes: >-
  Reference MCP server for Git repository interaction, giving LLMs tools to read, search, and manipulate repos.
